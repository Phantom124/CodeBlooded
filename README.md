# CodeBlooded

For the Monitors they must be deleted manually after everything.
Assumption: If a buyer returns a product, we assume that they return the product in the exact condition that we sold it to them.
After converting a snapshot into a plant, delete snapshot.