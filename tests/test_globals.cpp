/**
 * @file test_globals.cpp
 * @brief Global variable stubs for testing
 * 
 * Provides dummy implementations of global variables and functions
 * that may be declared in headers but used by GUI components.
 */

#include <string>

// Stub functions that might be referenced but not needed in tests
void initializeGlobalResources() {
    // No-op for testing
}
